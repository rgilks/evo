{
  "name": "evo-web",
  "version": "1.0.0",
  "type": "module",
  "description": "Evolution Simulation Web Application",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build-web.sh",
    "dev": "wrangler pages dev web",
    "deploy": "npm run build && wrangler pages deploy web --project-name evo",
    "setup": "./scripts/setup.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "wasm-pack": "^0.13.1",
    "wrangler": "^4.54.0"
  }
}
